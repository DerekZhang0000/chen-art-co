# Original, unedited source photos/videos - not used by the site.
# See README.md for why these are kept locally but not committed.
assets/

# OS cruft
.DS_Store
Thumbs.db

# Test dependencies (dev-only, never shipped)
node_modules/

.wrangler/

# Playwright run output (traces, HTML report) - not the committed
# tests/e2e/**/*-snapshots/ baseline images, which are checked in.
test-results/
playwright-report/

# Local secrets for `wrangler pages dev` - never commit real keys
.dev.vars

TODO.txt